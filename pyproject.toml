[project]
name = "stock-morning"
version = "0.2.0"
description = "SEC filing crawler and stock data agent system"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
]
